import './studio.css'

export const metadata = {
  title: 'Sanity Studio',
  description: 'CMS',
}

export default function StudioLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
