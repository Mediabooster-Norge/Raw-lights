# Navigation

`navigation` is a translated document. Fetch with `$locale`. Header and footer never hardcode links.

Internal `SanityLink` targets map the CMS homepage (and the fallback slugs `forside` / `home`) to `/` or `/en`.

The header includes a language switcher (desktop and mobile).
