# Integrations

Resend sends form mail. Configure `RESEND_API_KEY`, `FORM_FROM_EMAIL` and `FORM_TO_EMAIL`.

Sanity CDN images go through `next/image` (`cdn.sanity.io`).

Ticket providers, Mux, search and e-commerce are out of this starter.
