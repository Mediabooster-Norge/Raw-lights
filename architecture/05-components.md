# Components

UI: `lib/components/ui` (image, link, portable text, cookies, preview banner).

Layout: Header (with language switcher), Footer.

Blocks: registered in `lib/components/blocks/registry.ts` and rendered by `PageRenderer`.

Posts: `PostArchive` and `PostSingle`.

Forms: `FormRenderer` plus field components.
